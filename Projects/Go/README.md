# sample-go

Minimal Go module with a test.

Run tests:

```
go test ./...

Note: This project uses `github.com/stretchr/testify` as a test dependency; `go test` will download it automatically (or run `go get` / `go mod tidy`).
```

Publish:

```
git tag v0.0.1 && git push --tags
```
